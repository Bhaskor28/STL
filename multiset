		multiset

it can hold all value normally in ascending order 
if we erase by value it can delete all same type value
like if the multiset is {1 1 2 2 3 4}
and if we erase 2
ms.erase(2)

1.multiset declaration
2.insert()//always ascending
3.size()
4.max_size()// how many element i can hold
5.begin(),end()
6.empty()
7.erase()// here erase will work directly with value
another approach with position:
7.0:advance uses for making position
but if we want to get position we should use pointer
set<int>::iterator it;
it=s.begin();
advance(it,2);
s.erase(it);//0 base index


8.clear()
9.find()///let say we have iterator
// it=s.find(2)//here will store the index of 2

10.count()
11.lower_bound()
12.upper_bound()
13.emplace()
14.swap()
15.operator '='//by using this we can assign one set element to another


#if want to access this 
we should use iterator
